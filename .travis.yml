sudo: required

language: c

services:
  - docker

script:
  - make release
  - make check

deploy:
  provider: script
  script: make deploy
  on:
    branch: master

notifications:
  email:
    recipients:
      - natechancellor@gmail.com
    on_success: never
    on_failure: always
